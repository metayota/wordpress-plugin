class ViewTransaltedText extends Tag {
    set value(v) {
        if (v != this._value) {
            this._value = v
        }
    }

    get value() {
        return this._value
    }

    getTranslatedValue() {
        
    }
}